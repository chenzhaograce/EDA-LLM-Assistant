# Core Python libraries
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Config
pyyaml>=6.0.0

# Automated EDA
# Optional "extra profiling" engines.
# These packages often lag behind new Python releases and can fail to install on
# some deployment environments. The app only imports them when the user enables
# the corresponding Streamlit checkboxes.
# Install manually if you want those reports:
#   pip install ydata-profiling sweetviz

# Data visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# LLM API
openai>=1.0.0

# Report rendering
markdown>=3.6
tabulate>=0.9.0

# Web app / interactive UI
streamlit>=1.26.0

# Jupyter support (optional / dev-only).
# The Streamlit app does not require these packages, and they may not have
# wheels for the deployment environment's Python version.
# Install manually if you want notebook development:
#   pip install jupyterlab ipykernel